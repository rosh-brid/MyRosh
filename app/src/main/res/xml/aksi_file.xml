<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- internal storage -->
    <files-path
        name="internal_files"
        path="." />

    <cache-path
        name="internal_cache"
        path="." />

    <!-- external app storage -->
    <external-files-path
        name="external_files"
        path="." />

    <external-cache-path
        name="external_cache"
        path="." />

    <!-- external storage umum -->
    <external-path
        name="external_storage"
        path="." />

</paths>